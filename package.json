{
  "name": "to-do_postgres",
  "version": "1.0.0",
  "description": "A to-do app with database",
  "main": "app.js",
  "proxy": "http://localhost:5433",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
